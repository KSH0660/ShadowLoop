{
  "name": "shadow-loop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build:transcripts": "node scripts/build-transcripts.mjs",
    "check": "node --check server.js && node --check app.js && node --check scripts/build-transcripts.mjs"
  }
}
